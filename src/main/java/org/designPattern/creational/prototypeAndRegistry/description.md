It allows you to create new objects by copying (cloning) existing ones, instead of creating from scratch.
Clone an existing object instead of creating a new one
When to use : When object creation is expensive or complex 
Implement Cloneable and override clone() method
Prototype Pattern helps when creating an object is costly — it lets you make copies of existing objects quickly and efficiently.
